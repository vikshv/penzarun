import angular from 'angular';
import component from './component';

export default angular.module('app.components.pages.home.components.info.components.update', [
    ])
    .component('infoUpdate', component);
